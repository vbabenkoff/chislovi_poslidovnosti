#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
	double mas[50];
	double n;
	int i, t=10;
	srand(time(0));
	printf("Vvedite n:\nn=");
	scanf("%lf", &n);
	
	for(i = 0; i < t; i++){
		mas[i] =  (double) rand() / RAND_MAX * n;
		printf("%.2lf\n", mas[i]);
	}
	
	return 0;
